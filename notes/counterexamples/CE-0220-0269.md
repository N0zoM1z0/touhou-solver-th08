# TH08 Counterexamples CE-0220–CE-0269

Detailed original entries are recoverable from tag
`pre-workspace-prune-20260731`.

## CE-0220 — Power ledger order

**Observed/fixed:** the old ledger retained over-cap Power and reordered active
items. Native additions cap immediately at 128 and conversion/update follows
active-list order. Exact recurrence and exhaustive static ledger are the
regression.

## CE-0221 — Boss threshold overshoot

**Observed/fixed:** post-damage HP overshoot was decoded as next-phase
identity. Transition ordering, timeout, and selected successor state must be
carried explicitly.

## CE-0222 — Conditional successor writes

**Observed/fixed:** successor operands were treated as unconditional writes.
Only the executed native transition mutates the registry.

## CE-0223 — Incomplete Boss identity

**Observed/fixed:** phase identity omitted the selected successor registry.
Root identity now includes the complete stable successor state.

## CE-0224 — Lifecycle ring stride

**Observed/fixed:** x86 `imul ..., imm8` sign-extended event size `0x80` to
`-128`. The selector uses an imm32 encoding and a byte-level regression rejects
the old form.

## CE-0225 — Stale paired publication

**Observed/fixed:** per-site quiescence did not guarantee paired-hook
quiescence. Damage/pickup commits publish only when begin marker, pointer, and
manager frame match; otherwise replay exact native behavior without a serial.

## CE-0226 — Damage versus feedback

**Observed/fixed:** the 50-point hit-feedback clamp was misapplied to returned
ordinary-shot damage. The accumulators and writes are now distinct.

## CE-0227 — Ignored timeline events

**Observed/open boundary:** the integrated simulator silently skipped timeline
engine events. Any unconsumed event now returns `UNKNOWN`; no later state or
strategy authority may cross it.

## CE-0228 — Effective item type 7

**Observed/fixed:** item allocation scanned beyond the one native cursor
probe. The recurrence now performs exactly one probe and preserves cursor/list
effects.

## CE-0229 — Different-root physical totals

**Observed/open methodology:** the Stage 3/4A/5 ring changed hit totals
15→5, 10→13, and 19→12 on different RNG roots while WS-H ranking was disabled.
Aggregate deltas are workload evidence only. Use canonical first-hit roots,
same-root native branches, and repeated rotated physical trials for causal
promotion.

## CE-0230 — Early kill did not rescue global delivery

**Observed/open:** the default-off Stage-5 physical gate applied 27
same-direction unfocus preferences after fresh issue certification and
remained hard no-Bomb. First hit moved 2124→6981 relative to a different-RNG
baseline, but total hits changed 12→13. No decision received global allowed
actions or became viability-constrained. Early killing was physically
exercised but did not rescue the current global policy/kernel path in this
gate. This does not falsify the separate same-root native prevented-birth
witness; repeat on a second root and restore global publication.

## CE-0231 — Diagnostic scale horizon suppressed every global solve

**Observed/fixed:** the Stage-5 early-kill trace had 13,306 decisions and no
global publication. Player projection was not the blocker: 12,594 decisions
had exact zero-lag projection and 712 had exact one-step projection. The
diagnostic constant-scale schedule covered 120 frames, while the initial
rolling solve required `hazard age + 80` async-lead frames + 80 kernel frames
+ 1, or 161–162 frames in that trace. The submit predicate therefore rejected
every solve before worker execution. Diagnostic coverage now derives from the
maximum sensor age, async lead, and kernel horizon (269 frames), and each
decision records submit/publication gate state. Because this future schedule
is unknown-direction, resulting policies are queryable shadow evidence only
and cannot constrain physical actions. A rotated physical gate must confirm
actual submit, completion, publication, and query.

## CE-0232 — Local-safe early kill is not global survival filtering

**Observed/open:** the repaired Stage-4A physical gate delivered 1,915 unique
policies and 12,156 available queries, but 6,635 were losing/empty and every
hit followed global-kernel exhaustion. Early kill selected 39 actual
fresh-safe preferences: 26 were already in a losing shadow-global state, and
only nine were also inside a winning shadow-global action set. The first
attempt lost its last winning query 356 frames before the hit; eight early-kill
preferences occurred after that loss. One empty-fresh-set least-bad selection
coincidentally matched the requested peer and was incorrectly labeled
`preference_applied`; telemetry now requires actual preference eligibility.
This does not falsify early killing. It falsifies treating a short fresh-local
certificate as the intended global survival filter.

## CE-0233 — HP-gated observation missed the middle wave before exhaustion

**Observed/open:** in physical Stage-4A run `20260731_133852`, every one of
124 applied early-kill preferences before first hit 4148 belonged to a
winning queried shadow-global set and passed fresh issue certification. The
last winning query was frame 3679. Enemy `0x006443D0`, maximum HP 200, was
body-visible near `(319.4, -0.7)` at decision frame 3864 but the old observer
did not select it until frame 3900 at 15 HP. The shipped Stage-4A timeline
contains the corresponding fixed x=320 birth at time 3860; observed bodies
lagged fixed births by two to four manager frames.

This falsifies current HP `<=22` or maximum HP `<=30` as the observation
boundary for pre-exhaustion combat positioning. It does not authorize local
guidance after kernel loss. The attempted correction combined full-health
ordinary targeting with a live-clock/runtime-byte-verified fixed-spawn
forecast; CE-0235 later falsified and disabled the forecast portion.

## CE-0234 — Queried global recovery had no action authority

**Observed/open:** physical Stage-4A run `20260731_142342` produced 11,947
available global queries, including 5,341 winning states and 4,119 losing
states with distant recovery, but constrained zero decisions and selected zero
recoveries. At the canonical nonspell hit 1915, the last winning query was
frame 1707 with safe actions `stay/up/up_fast`; the physical transaction
selected fresh-local-safe `down_left`, outside that set. Global loss followed
at 1710, while the fresh issue set did not become empty until 1912.

This is an exact action-authority counterexample, not merely a short horizon:
the queried policy contained an earlier survival restriction but the live
ordinary-stage path intentionally stripped it. Later spell phases are
discovery-only, but reinforce the symptom: spells 57/65/69/73 exposed
54/235/562/386 distant recoveries and selected none.

**Rejected mitigation:** CE-0237 shows that the first default-off
ordinary-nonspell reserve filter never activated physically and would still
permit a canonical losing action after correcting only its state gate.
CE-0234 remains open; it now requires set-valued hazard-space authority rather
than scalar boundary reserve.

## CE-0235 — Timeline forecast recycled a completed/startup birth

**Observed/open:** the same physical run emitted 376 byte-verified spawn
observations, but every one named timeline 0, time 1, x=30, y=-16. Only three
preferences were applied, at frames 401/519/528; no later wave was forecast.
The forecaster maps a zero runtime instruction pointer to the static timeline
start and has no lifecycle bit distinguishing not-yet-started from completed.
The trace therefore physically falsifies this implementation as a general
later-wave early-kill observer. Preserve full-health observed-body targeting;
make clock lifecycle causal before forecasting again.

**Mitigation implemented:** the live early-kill path no longer constructs or
calls the timeline forecaster. Forecast code remains offline/testable but has
no objective or action authority.

## CE-0236 — A new bullet invalidated a positive local certificate

**Observed/open discovery:** at spell-57 hit 13689, the last alive decision at
13686 reported pipeline clearance `+47.357` with 578 bullets. Three physical
frames later the pool held 581 bullets and slot 1149 overlapped at clearance
`-0.596`; its retained velocity could not traverse the earlier 47-pixel gap.
The global query also lacked future-hazard authority. This is consistent with
an unseen hostile birth/transform, not a bad tie-break among the 578 observed
bullets. Nonspell hit 22814 separately retained positive `+1.005` pipeline
clearance with no exact bullet/laser/body contact candidate and remains a
sensor-or-unmodeled-hazard case.

## CE-0237 — The ordinary reserve authority never activated and is not equivalent

**Observed/open:** user-authorized Stage-4A run `20260731_152921` completed
hard no-Bomb with 17 hits and first hit 914, but the enabled pre-exhaustion
experiment produced zero eligible/applicable/effective decisions. Every one
of 7,202 nonspell decisions failed `player_transition_or_predeath`; early kill
therefore applied zero preferences. Native `0x0044AB40/0x0044C390/0x0044C650`
and 5,862 normal phase-0 trace rows show player `+0xE2A68` is a retained
deathbomb-window limit, not a zero-when-alive predicate.

Removing only that gate offline still permits `down_left` at global
exhaustion frame 835. At frame 850 an uncontrollable no-pickup prefix makes
all 17 actions share worst reserve zero; at the saturated corner frame 910,
playfield clamping again aliases all 17 actions at reserve zero. Bullet slot
455 first entered the retained nearby set at decision 817 while the older
hazard-snapshot policy kept certifying `down_left` through frame 833; the next
snapshot delivered an empty kernel at 835, 79 frames before impact. This
compact trace cannot attribute the policy-version flip to slot 455 alone.

This falsifies both the live eligibility wiring and the claimed equivalence
between scalar boundary reserve and ordinary global viability. Do not rerun
after only deleting the stale-counter check. The next candidate needs a
set-valued, hazard-space pre-publication predecessor across pickup order,
publication lead, and bounded future births. Stage 5 was correctly skipped.

## CE-0238 — Signed predecessor implemented; birth coverage closes the gate

**Observed/open:** the replacement enumerates the exact observed
active/held/pending pickup branches through a fresh signed-clearance prefix,
then queries retained full-horizon per-action safety values at the completed
future publication. It subtracts off-grid position error and requires exact
coverage interval/version equality. Player phases 1/2 fail closed; phases 0/3
remain native movement phases. Focused tests and complete Linux discovery
pass (1,196 tests).

The compact retained f817/833/835/850/910 gate preserves the physical
falsifier. `left_fast` is the minimum-distance recovery at f835 and f910,
while scalar reserve still ties all 17 actions at f850/f910. However, the
source run predates retained signed per-action safety values and all five
future-hazard slabs are `model_unknown` for unseen births/events. Therefore a
nontrivial exact allowed set cannot be established from this source and the
hard gate is false. The authorized Stage-4A physical trial was correctly not
run.

**Decision:** keep the new flag default-off and fail closed. Next establish
bounded ordinary birth/event coverage over the current-prefix plus
future-policy interval; do not substitute the falsified timeline observer or
tune local ranking.

## CE-0239 — Future-source coverage must be geometrically consumed across publication

**Observed/fixed:** strict branch-mode native capture at retained Stage-4A
f817 failed closed twice because the Windows thread epoch changed after a
headless action branch. A new observation-only root mode freezes non-owner
threads, captures the immutable replay root without changing gameplay input,
checks the epoch before/after, and cleans up. It produced coherent
collision-control v12 roots at observation frames 816/831/834/848/908 from
accepted replay SHA-256 `4588af47384d38ed...`.

**Observed native semantics:** the first f817 source closure stopped at main
ECL opcode `0x8B`. Shipped code at `0x0041D01D` allocates effect type 51;
its audited init/update callbacks `0x00426280/0x004264F0` mutate visual ANM
state and emit no enemy, bullet, laser, item, or timeline producer. The next
stop, opcode `0x4A`, installs native polar motion state 3 with duration,
angular velocity, radius acceleration, and orbit center. Collision-control
projection v12 now captures those fields and the source executor reproduces
that recurrence. Main opcode `0x04` is a literal timer/jump loop and is also
executed exactly. Any unaudited effect/opcode/callback/motion still returns
`UNKNOWN`.

**Observed/fixed coverage:** every retained root closes for the live
268-frame horizon with 12 sources, one reachable timeline spawn, and one
future hostile-body AABB trajectory; the roots contain 316/284/276/248/141
current bullets and no lasers/contact-enabled current bodies. A separate
Stage-5 synthetic wave closes nine timeline spawns and more than 1,000 future
fire events. Future fire is consumed as continuous annular sectors.

**Observed implementation gap:** initially the future projection was used
only after the corridor publication epoch. The active/held/pending prefix
still queried current bullets/bodies only, so a future birth inside the
publication lead could be omitted despite a `complete` coverage record. The
exact projection is now retained as a process-local policy handle and its
annular-sector/AABB geometry is queried on every prefix physical frame. An
interval/version match without that geometry cannot produce authority.

**Observed deterministic result:** the rebuilt f817/833/835/850/910 chain has
complete exact-version coverage and zero unavailable signed terminal
branches at all five roots. At f817 the exact allowed set is
`stay/left/down/down_left/down_right/left_fast/down_fast/down_left_fast`;
active `up_fast` has margin `-16.137`, issued `up_left_fast` has `-18.845`,
and best `stay` has `+21.280`. f833/f835 retain nonempty downward sets. The
predecessor first becomes empty at f850; by f910 the fresh publication prefix
is also empty. The pending f850 root preserves four no-write branches versus
28 write branches.

**Authority/decision:** this closes CE-0238's deterministic birth/event
blocker; it is native-replay regression evidence, not a physical outcome.
Linux and Windows complete discovery each pass 1,226 tests (Windows skips
three), and both native builds pass the 42-symbol ABI gate. The default-off
feature may proceed to the already authorized single Stage-4A physical
falsifier. Empty predecessors remain fail closed and local ranking remains
later.

## CE-0240 — H=32 signed authority was erased at the live horizon

**Observed:** `--ordinary-preexhaustion-authority` forces the complete
80-frame corridor horizon. Recomputing CE-0239's retained chain at H=80 makes
the 16px signed predecessor empty at f817/833/835/850/910. The former f817
eight-action set and f833/f835 directional sets were H=32 artifacts. Future
source closure, geometry consumption, and version equality still pass, so
this is a terminal-kernel resolution failure rather than a new source
coverage failure.

**Invalid assumption:** a coarse signed scalar terminal value could preserve
the directionally recoverable set at the real horizon. It cannot: the 16px
terminal lattice erases the overlap even though a 4px recurrence retains a
winning route.

**Correction:** ordinary nonspells now use a 4px Boolean lower kernel with
the 2.828px cell radius consumed as required clearance. A causal predecessor
holds each selected complete mask with no later write across all old/new
pickup branches into the next queryable active-policy layer. A pending
terminal command must be a member of that layer's safe action set. Missing
source coverage, wrong version/resolution, an expired layer, or an empty set
fails closed.

**Focused regression:** observation-aligned roots 816/831/834/848/908 have
complete source closure and no unresolved action. Native exact belief
recurrence and the live adapter independently return only
`left_fast/down_left_fast` at f817, excluding active `up_fast` and issued
`up_left_fast`; the later four roots remain empty. Windows measured f817
kernel/certificate time at 1420/21 ms. Evidence:
`artifacts/runtime_reports/th08_ordinary_factorized_prefix_gate_20260731.json`.

**Physical authority:** finite-model and Windows delivery evidence only. No
new physical trial had run when this counterexample was recorded. CE-0239's
H=32 action-set conclusion is superseded; its future-source/native-semantics
evidence remains valid.

## CE-0241 — Sparse live source capture starved exact ordinary authority

**Observed physical:** Stage-4A run `20260731_220830` completed with 23 hits,
first hit 939, and zero Bombs. Its different-RNG aggregate cannot reject the
earlier local improvement. The causal activation result is stronger:
0/1,754 future-source projections completed, so all 10,016 ordinary
prepublication records failed closed and zero actions were constrained.
1,726 captures crossed `enemy_manager_frame`; the residual reached errors
were torn auxiliary depth, uncaptured indexed timeline state, dynamic fire
count, auxiliary `0x1A`, and FRScreen blocking.

**Invalid assumption:** a manager-frame bracket could make at least 481
separate process reads serviceable often enough for rolling hard authority.
The bracket correctly rejected crossed roots, but the capture topology made
rejection nearly universal. The 23-hit fallback outcome therefore does not
falsify CE-0240's exact f817 action set.

**Correction:** one contiguous 9.8 MiB read now captures the manager singleton
and all 480 ordinary slots. Source semantics v3 lowers each reached residual
case above, while any still-unsupported event remains `UNKNOWN`. The exact
Boolean solver checks terminal membership before branch paths and recognizes
provably all-clear slabs/layers. Hard membership avoids eager soft scans;
directional recovery remains when the exact set is empty.

**Focused regression:** the Windows retained chain preserves complete
coverage, no unresolved action, and exactly
`left_fast/down_left_fast` at f817. No-hazard viability is 281.7 ms cold and
8.9..9.3 ms warm versus 2,995.6 ms in the physical bootstrap solve. Dense
retained maximum remains 2,103.6 ms and expires fail closed.

**Physical authority:** correction is deterministic/Windows validated but
not yet physically promoted. The next Stage-4A gate must first establish
nonzero complete live source projections and authority-eligible decisions.

## CE-0242 — Contiguous live capture still copied longer than one frame

**Observed physical:** the correctly configured Stage-4A run
`20260731_231944` completed with 35 hits, first hit 498, and zero Bombs, but
only 1/1,859 future-source projections completed. 1,836 roots crossed
`enemy_manager_frame`; the sole complete root occurred during a freeze and
never reached hard policy authority. All 4,499 state-eligible nonspell
records remained `future_policy_unavailable`, including all 332 decisions
before the first hit. The preceding `20260731_230657` replay is separately
invalid for this experiment because its launch omitted the runtime ECL image
and produced zero source attempts.

**Invalid assumption:** one contiguous `ReadProcessMemory` call was sufficient
to make the complete bracket short. `ProcessReader.read` also allocated and
zeroed a 10.32 MiB ctypes destination, copied it through `.raw`, and sliced
another pool-sized bytes object while the manager clock remained bracketed.
The successful capture measured 16.118 ms, effectively one physical frame.

**Correction:** allocate one destination before the bracket and retain it on
the dedicated future-source worker. Read directly into that buffer and pass
manager/pool memoryviews without copying. Windows local medians for the
removed allocation/`.raw`/slice work are 3.74/3.43/3.40 ms versus 0.004 ms
for the view. Source semantics, manager-frame equality, and every fail-closed
coverage check are unchanged.

**Gate:** focused Linux source/projection tests and the focused Windows
capture test pass. A fresh physical gate must show a material complete-root
rate and nonzero authority eligibility/effectiveness before hits or Stage 5
are interpreted.

## CE-0243 — Live authority activated only in trivial safe roots

**Observed physical:** Stage-4A `20260731_233856` completed with 24 hits,
first hit 845, zero Bombs, accepted replay, and cleanup. Persistent read-into
capture produced 119/1,914 complete source projections. Exact ordinary
authority was eligible 205 times, applicable 196 times, and effective at issue
193 times. This closes the former capture/delivery activation blocker.

**Counterexample:** 172 allowed sets contained all 17 actions; only 24 were
nontrivial and nine were empty. Before the canonical hit, all 98 applicable
sets were trivial all-safe sets and 295 roots were unavailable. No nonempty
effective authority existed within 80 frames of any hit. Therefore nonzero
authority counts do not establish useful pre-exhaustion coverage.

**Observed cause:** source closure failed chiefly on 699 manager-frame
crossings, 476 legal auxiliary timer roots, 237 dynamic float-`10069` reads,
and 181 armed phase transitions. Native `ecl_eval_float` at `0x0042067F`
reads `10069` from captured enemy angle `+0x2D94`. Native auxiliary scheduling
uses the independent VM `+0x90` delay timer; opcode `0x02` resets it from an
evaluated integer operand and consumes one tick in the same update.

**Correction/gate:** projection v13/source v4 retains that delay timer,
accepts prior/fractional unit-scale roots, lowers `10069`, and executes generic
delay resets. The f817/833/835/850/910 factorized regression still passes with
only `left_fast/down_left_fast` at f817. Armed phase successor coverage remains
fail closed and must be integrated before the next physical promotion gate.

## CE-0244 — Exact source became complete while UNKNOWN publication blocked it

**Observed physical:** Stage-4A `20260801_002006`, based on `7558f47`,
completed with 24 hits, first hit 1205, zero Bombs, accepted replay, and full
cleanup. Source v4 removed the former `10069` and legal auxiliary-delay-root
failures, but only 159/1,942 projections completed. Exact ordinary authority
was effective on 135/6,146 nonspell decisions and nontrivial on 16. It was
effective on 0/383 decisions in the 80-frame windows before 14 nonspell hits.

**Observed causal delivery failure:** complete projections existed continuously
at roots 1137..1200 before the first hit. Nevertheless, an UNKNOWN-source
kernel that cost 1,403 ms was staged for future frame 1186 and blocked new
submission. The next 737 ms solve was not submitted until frame 1190. The
pre-publication path inspected only the active policy, even though its intended
terminal object is the already-completed exact pending future policy.

**Correction:** ordinary mode now submits a solve only when a complete source
projection covers the proposed policy source plus horizon. A completed exact
pending policy participates in terminal-kernel selection before its source
epoch; the earliest exact active/pending terminal is chosen, and its complete
projection still certifies every root-to-publication frame. Incomplete source
coverage continues to fail closed and no Bomb semantics changed. Focused Linux
and Windows controller/prepublication tests pass. The next physical gate must
show pending-terminal use and useful authority near pressure; source phase,
transform, opcode, callback, and movement gaps remain open.

## CE-0245 — Pending delivery activated, then sector clearance missed its epoch

**Observed physical:** Stage-4A `20260801_004533` completed with 22 hits,
first hit 1837, zero Bombs, accepted replay, and cleanup. Exact ordinary
authority was effective on 287/7,275 nonspell decisions; 110 effective roots
used a completed pending policy, including 43 before the first hit. This
closes CE-0244's pending-publication activation question. It remained
effective on 0/287 decisions in the 80-frame windows before 11 nonspell hits.

**Observed causal bottleneck:** complete sources carried up to 750 future
annular-sector trajectories. Representative 6,939/19,783/23,460 ms exact
solves spent 6,103/19,506/23,199 ms in signed-clearance construction but only
810/237/227 ms in Boolean viability. The blocker is therefore the exact
sector-volume algorithm, not recurrence or a scalar-reserve defect.

**Correction:** the native sector kernel groups samples with identical
origin, radial interval, and inflation, computes their exact circular angular
union, and evaluates each lattice point once per group. Independent sectors
still remain separate. Frames are distributed over at most 16 native workers;
each frame owns a disjoint output slab. A 750-trajectory, 81-frame, 4px
pressure regression completes in about 198 ms on Linux and matches the
independent Python signed-clearance oracle exactly at sampled frames. Eighty
random wrap/overlap/disjoint union cases also had zero observed error.

**Gate:** focused Linux and Windows native parity tests pass. The retained
f817/833/835/850/910 semantic chain is unchanged: f817 permits only
`left_fast/down_left_fast`, the later roots are empty, and no action is
unresolved. Physical promotion remains pending; missing source coverage still
fails closed and Stage 5 remains withheld.

## CE-0246 — Sector acceleration exposed exact recurrence publication latency

**Observed physical:** Stage-4A `20260801_011902`, based on `e8b77a8`,
completed with 26 hits, first hit 796, zero Bombs, accepted replay SHA-256
`fb9718c7b1f13eac6f00d8a53be08f90f4a24beb45520a049c395f03842fb3f3`,
and full input/process cleanup. The different-RNG hit count is observational.
Exact ordinary authority was effective on 399/7,162 nonspell decisions but
0/380 decisions in the 80-frame windows before 15 nonspell hits.

**Observed causal split:** maximum clearance construction fell from the prior
physical 23.2 seconds to 0.993 seconds, validating CE-0245's sector change.
For the canonical frame-796 hit, exact source 639 spent 478 ms in clearance,
918 ms in Boolean viability, and 1,415 ms total. Its policy arrived after the
corridor's last nonnegative gate at frame 714; the predecessor was empty at
frame 716. Armed phase-transition coverage failed 1,195 projections and is a
later aggregate blocker, but it is not the first-hit cause.

**Correction:** the native Boolean recurrence proves common all-action
interior states using a box that contains every active/selected/pickup-delay
trajectory at every physical step. The shortcut requires every cell in each
box to exceed exact signed clearance and every terminal action to be viable;
unproved states retain the original sample-wise recurrence. On the Windows
retained f817/833/835/850/910 chain, action sets and unresolved status are
unchanged while maximum solve time falls from 2,071.371 to 774.643 ms.
Physical deadline improvement remains unproven until the next Stage-4A run.

## CE-0247 — Faster recurrence handed the canonical hit to transform coverage

**Observed physical:** Stage-4A `20260801_015631`, based on `627d937`,
completed with 20 hits, first hit 2009, zero Bombs, accepted replay SHA-256
`b6c1dbbc608ee632dd0e4d3ba72b8cefe0795b0600020682ac0b157610c66d73`,
and full cleanup. Nonspell hits were nine. Different-RNG count changes are
observational.

**Observed correction effect:** across 43 unique complete nonspell policies,
solve maximum fell 3,050→1,338 ms. Viability maximum/p95 fell
3,026/1,664→1,038/926 ms. Authority was applicable/effective on 494/479 of
6,941 nonspell decisions. Before first hit, 278 decisions were applicable and
102 were directional rather than all-17.

**Observed remaining failure:** authority was 0/238 in the 80-frame windows
before nine nonspell hits. All 33 decisions before canonical frame 2009 were
`future_policy_unavailable`. Source roots 1915..2007 chiefly failed on
nonzero transform programs; opcode-`0x19` also recurred. Aggregate projection
failures still include 903 armed phase transitions, but those do not explain
this first window.

**Correction:** shipped `0x0042FFC0` executes only transform records whose
kind intersects original bullet flags. Source v5 lowers active `0x80`
stop/re-aim as a conservative full-direction path-length disc bounded by
emitted/resume speed, retaining future-player uncertainty rather than
guessing it. Barrier/cull-suppression/sound records do not expand motion;
other active kinds remain UNKNOWN. Auxiliary `0x19` now uses exact interval
addition. Focused Linux/Windows tests and the retained five-root action sets
pass; physical pressure-window coverage remains pending.

## CE-0248 — Directional pressure authority arrived but missed issue deadline

**Observed physical:** Stage-4A `20260801_022228`, based on `19066af`,
completed with 19 hits, first hit 1833, zero Bombs, accepted replay SHA-256
`09e8b017928315133c891aed838e3d681750657583d15cdb2536c439a5f3d93d`,
and full cleanup. Nine hits were nonspell; aggregate counts are different-RNG
observations.

**Observed coverage result:** transform-program and opcode-`0x19` failures
fell to zero. The next reached classes are armed phase transitions 913,
installed callbacks 288, and auxiliary conditional `0x2E` 258. Exact
authority was applicable/effective on 480/461 of 6,994 nonspell decisions.

**Observed canonical delivery failure:** the 237 nonspell hit-window decisions
contained three applicable directional sets, sized 5/8/6, but zero effective
sets. At frames 3501/3518/3532, post-capture advance 15/16/12 exceeded
pickup-support high 6 and the deadline guard held `up_right`; frame 3501's
five-action set excluded that held input. This is the first physical evidence
that v5 reaches pressure with useful directionality, and also falsifies mere
applicability as sufficient action authority.

**Correction:** ordinary background solve ownership is below-normal and its
native worker cap is 8 instead of 16, preserving game/sensor/issue CPU. The
Windows retained maximum rises 804→1,095 ms, but f817 remains exactly
`left_fast/down_left_fast`, later sets remain empty, and no action is
unresolved. Auxiliary `0x05` exact decrement/jump and `0x2E` exact integer-LE
jump close the newly reached loop. Physical effective-delivery validation is
pending.

## CE-0249 — Delivery QoS exposes far-timeout false blockage

**Observed physical:** Stage-4A `20260801_024419`, based on `223ba01`,
completed with 22 hits, first hit 1291, zero Bombs, accepted replay SHA-256
`8353c74ed6fd612996dd9df76b6a230ae5615802571a3e3093849362dedd0d5b`,
and full cleanup. Thirteen hits were nonspell; different-RNG totals are not a
causal A/B result.

**Observed delivery result:** eight-worker/background-low-priority execution
was active. Ordinary solve median/p95 was 123/218 ms versus 123/230 ms in
`022228`, but one solve took 5,290 ms and only 26 ordinary policies completed.
Applicable/effective authority was 380/362 of 6,611 nonspell decisions. The
236 nonspell hit-window decisions had two directional 14-action sets and zero
effective directional sets; both arrived 20 frames after capture against
pickup-support high 6.

**Observed coverage result:** `0x05`/`0x2E` failures fell to zero. Armed phase
transition on ordinary slot 0 became 1,243 of 1,714 incomplete projections,
the dominant blocker.

**Correction:** IDA recheck at `0x42B930` confirms timeout compares only the
integer phase timer using `>=`; `0x42B490` confirms signed HP `< threshold`.
Both start the registered successor. Source v6 captures the four health
successors and exact phase timer, then suppresses only a timeout proved beyond
the bounded horizon (`elapsed + H < timeout`). Any armed health threshold or
reachable timeout remains fail closed. Linux/Windows focused tests and the
five-root exact chain pass unchanged with no unresolved action.

## CE-0250 — Physical computation latency expires an offline-correct lease

**Observed physical:** authorized Lunatic Stage-5 runs `20260801_092136` and
`20260801_093817` completed 15-hit/first-1863 and 20-hit/first-1873,
respectively, with zero Bombs, accepted replays, and full cleanup. Aggregate
hit counts use different RNG roots and are observational. Both canonical hits
were ordinary nonspells and retained positive local pipeline clearance.

**Observed delivery mismatch:** in `092136`, frame 1847 used snapshot 1777.
The all-17 terminal predecessor probe alone took 796.649 ms; observe-to-input
took 1,172.187 ms and the issue was 70 manager frames old. The broad future
kernel was empty, while the H80 action-conditioned held witness failed to run
because the retained projection had only 52 complete frames remaining. This
directly proves that offline/native geometry parity does not include live
sensor, query, publication, and issue latency.

**Correction and physical result:** terminal work now queries only an exact
three-action subset (held plus two nearest recovery actions), reuses that
terminal result after the prefix certificate, and never lets local/fresh issue
logic escape a named exact authority. The held no-write fallback consumes the
complete remaining projection horizon and is judged against actual issue age,
not the new-command pickup deadline. In `093817` it was physically effective
seven times; at frames 1686..1748 it certified held `right` through H80 despite
24/27/12/19-frame issue ages. From frame 1767 it correctly became unsafe, 106
frames before hit 1873. The probe still cost 126.181 ms median and up to
637.836 ms; no exact planning-lag transition existed to replace held input,
so the controller continued `right+deadline_hold` and hit.

**Next falsifier:** build the action-conditioned causal predecessor across the
complete computation/issue delay, old pending command, new write pickup, and
publication lead. It must find and physically issue a safe alternative before
the retained held witness turns unsafe. Do not tune scalar reserve or local
ranking first.

## CE-0251 — Delayed predecessor became live; candidate work spent its lease

**Observed implementation contract:** ordinary nonspells now build an H80
table indexed by the finally observed snapshot-to-issue age. During
computation the active input and any old pending command evolve without a new
write. At issue, held complete input is no-write and preserves pending state;
a different complete mask replaces pending state and ranges over pickup delay
0..6. Future ECL aim is conditioned separately on each selected action's
hidden paths. Missing source/event coverage, phase 1/2, age after the terminal
row, or nonpositive signed clearance fails closed. A fresh issue-time hostile
body slab intersects every row before the final age read.

**Observed deterministic gate:** packed binary32 path propagation is bitwise
equal to the scalar native-order movement oracle. The retained
f817/833/835/850/910 roots have complete tables and exact no-write/write branch
counts. With q=0..24, elapsed time is 120/112/104/269/103 ms; the saturated
roots remain empty except retained f833 `right_fast@q24`. This preserves the
counterexample instead of manufacturing viability.

**Observed physical integration sequence:** Stage-5 `20260801_111208`
physically activated the full-age/fresh-body table three times, but its
canonical pre-hit scan computed three actions for 1,282 ms and reached q81;
`left_fast/right_fast` were safe only through q49. Incremental independent
action evaluation in `112918` produced 11 effective scans. Its first-hit root
proved the remaining ordering error: `right_fast` was safe only q15..24 but
was evaluated third, at q59, and final issue was q62. Prioritizing the already
observed local proposal made the correct candidate first, but repeating that
same immutable local plan after the predecessor still expired some rows.

**Correction and physical result:** the local proposal now orders computation
only and has no action authority. Each exact action predecessor is evaluated
incrementally and work stops only when that action is safe at the
non-authoritative intermediate age. Final authority still requires the fresh
body intersection and exact observed row. The immutable ranking proposal is
then reused; fresh issue independently recertifies all local actions, so no
second stale-root plan is needed. Stage-5 `20260801_115838` completed with 18
hits, 10 nonspell, zero Bombs, accepted Route-2 Lunatic replay SHA-256
`00d8cd9839a5f972b1be144e3b7314e5c4e5afca473bcfbb974e9568d446fcfb`,
and full cleanup. Eleven of 21 delayed scans were effective. Median issue age
was 25 frames versus 43 in ranking-only `114613`; median delayed computation
was 342.9 versus 494.8 ms. Different-RNG hit counts are observational, but
the exact authority conversion and latency reduction are physical mechanism
evidence.

**Remaining mismatch:** this closes CE-0250's zero-transition blocker, not
ordinary NMNB. `115838` still had 10 nonspell hits. Some rows are exactly
empty at final age; long sequences of unsafe candidates can still exceed H80;
fresh body changes can remove a narrow row. The next general experiment is a
lease/terminal-continuation predecessor that preserves an effective selected
direction until a new exact replacement is available, then a rotated Stage-3
or 4A physical workload. Do not return to scalar reserve or treat local
ranking as survival authority.

## CE-0252 — One-step exact direction was discarded before its H80 witness

**Observed retained physical sequence:** Stage-5 `20260801_115838` built an
exact held/no-write `left_fast` predecessor from snapshot 1456 and issued at
1469 with positive minimum clearance 48.943. Its binary32 path predicts the
next snapshot-1470 player position `(45.8077545, 402.4606323)` exactly, with
active and held input both still `left_fast`. The former controller then used
an unconstrained local iteration to select `right_fast` by snapshot 1479.
That switch had no retained delayed predecessor and discarded a still-live
finite witness.

**Implemented correction:** an effective delayed final-age row creates an
immutable terminal-continuation lease over its exact content-addressed
projection version and digest,
pipeline root, issue/pickup support, branch positions, fresh geometry seam,
and H80 expiry. Each compatible fallback root first evaluates held/no-write;
only if it is unsafe are other directions evaluated as independent exact
predecessors. Held issue-age rows share the same physical path and are
computed once. Local coarse relaxation cannot widen delayed or lease sets.
Any context, phase, pending-support, position, expiry, or fresh-geometry
mismatch revokes the lease. Final issue rereads native spell-active and
stage-route, closing the capture-to-issue ordinary-context seam.

**Observed deterministic falsifier:** f1456/f1469 remains compatible at
f1470, while the old f1479 switch fails `held_action_mismatch`. The retained
f817/833/835/850/910 actual issue rows remain empty; f833's isolated q24 row
does not authorize observed q0. Linux and Windows focused gates pass.

**Remaining gate:** this is not physical promotion. Rotate to Lunatic Stage 3
or 4A and require a nonzero lease create/renew/consume sequence or an explicit
fail-closed revocation, with no Bomb and full cleanup.

## CE-0253 — Native input publication was one frame ahead of movement

**Observed physical:** Stage-4A `20260801_130742`, based on `f197899`,
completed with 21 hits, 12 nonspell, zero Bombs, an accepted Route-2 Lunatic
replay, and full cleanup. Six exact leases were created and 34 were renewed;
three rows selected lease authority, but zero leases survived to effective
physical no-write consumption. Three revocations reported fresh geometry and
three reported a pipeline/position mismatch. Different-RNG hit totals are
observational.

**Observed native contract mismatch:** delayed write f3191 was computed from
snapshot 3174 and changed `down_right_fast` to `down_right` at q17. Snapshot
3192 observed native current input `down_right`, while player position
`(248.5686035, 432.0)` was bit-exactly one additional
`down_right_fast` movement frame. Shipped `player_update_input_movement`
`0x0044AEC0` reads current input at priority 9; `replay_record_input_frame`
`0x00452310` publishes raw input at priority 17. Therefore publication at
frame f is first consumed by movement at f+1. The old branch incorrectly used
the published action for both observations and same-frame movement.

**Correction:** TH08 roots now carry a one-frame input-publication-to-motion
lag. Hidden branches retain separate published and movement action histories;
lease observations condition on the former and binary32 positions on the
latter. The f3191→3192 witness now matches exactly. Lease v2 also replaces
the coarse “any hostile-body change revokes” rule with a fail-closed,
pointer-independent set test: each fresh uncertainty/robustness-expanded body
AABB must remain contained at every remaining frame in an active-body or
future-source AABB already consumed by the old exact proof. Removal is safe;
an uncontained new or divergent body revokes with its first pointer/frame.

**Observed deterministic gate:** Linux focused tests and 186 Windows focused
tests pass. The Windows f817/833/835/850/910 delayed chain retains complete
coverage and exact branch counts; every actual q row remains empty and cannot
create a lease. The isolated f833 `right_fast` row moves to q23 but its actual
q0 row is still empty. Rotated Stage-3 physical validation remains required.

## CE-0254 — Finite unit-scale timeline fraction starved Stage-3 authority

**Observed physical:** Stage-3 `20260801_134853` completed with one ordinary
hit at f7935, zero Bombs, Power 128 throughout, accepted replay, and cleanup.
The old Stage-3 workload had five hits, but different RNG prevents causal A/B.
All 4,725 eligible ordinary roots lacked a future policy; lease create, renew,
select, and effective counts were zero. Of 1,214 source projections, 701
failed on `timeline 2 has fractional time` and 465 crossed the clock bracket.
All 49 source attempts in f7695..7935 failed on the fraction, so recursive
authority had no opportunity to affect the first hit.

**Observed native contract:** source analysis already requires exact unit
time scale. Native timer advance at `0x00447421` then takes its fast path,
increments signed integer elapsed, and preserves the float fraction bits.
Timeline scheduling observes integer elapsed only. Any finite fraction is
therefore causally inert in the reduced unit-scale timeline clock; requiring
zero was a false UNKNOWN.

**Correction/falsifier:** source semantics v12 validates both timer components
and retains integer elapsed while discarding a finite fraction only after the
unit-scale root is proved. NaN/infinity and nonunit time remain fail closed.
Focused timer/timeline/future-source and integration tests pass. A repeated
Stage-3 run must show nonzero complete publications and lease activity; a low
hit count without those mechanisms cannot promote recursive authority.

**Observed repeat:** Stage-3 `20260801_141250` completed with four hits (three
ordinary, one spell 50), zero Bombs, accepted replay, and cleanup. Source v12
produced 692/1,261 complete roots, 596 submissions, 595 completions, and 265
effective exact ordinary issues. Four effective delayed rows created four
leases, but all were revoked by fresh-body containment and zero leases were
effective at issue. Canonical hit f3181 had no proximate exact authority: the
last effective issue was f1406 and all 103 decisions in f2941..3181 reported
`future_policy_unavailable` despite 14 complete captures. This validates the
fraction correction as a physical delivery mechanism, not as hit reduction or
recursive viability. Different-RNG 4-versus-1 is observational.

## CE-0255 — Stage-3 one-hit did not generalize; leases died before use

**Observed physical:** parent checkpoint `5fc6d92` Stage-4A
`20260801_142851` completed 18 hits/10 nonspell, zero Bombs, accepted replay,
and cleanup. Its direct `130742` baseline had 21/12, but first hit was nearly
unchanged at 833 versus 819. Stage-5 `20260801_143952` completed exactly the
same 18/10 as direct baseline `115838`, with first hit 1888 versus 1728, zero
Bombs, accepted replay, and cleanup. Different RNG makes these hit deltas
observational, not causal A/B evidence.

**Observed common mechanism:** Stage 4A created nine leases and Stage 5 seven,
but effective no-write consumption was zero in both. Fresh-geometry
containment revoked seven of nine Stage-4A leases and three of six recorded
Stage-5 revocations, matching all four revocations in Stage-3 v12 `141250`.
The retained Stage-3 one-hit run had no source coverage or lease activity and
was fallback luck, not recursive global viability.

**Inferred mismatch:** lease creation certified active bodies with a robust
linear future. Every fresh observation then restarted a second linear future
for the full remaining H80 and required it to fit inside the old one. Native
timed acceleration/deceleration can make that restarted approximation diverge
even when the causal ECL trajectory remains covered. The correction must add
all active hostile source trajectories to the exact projection and compare a
fresh observation at its observation frame. Missing source/topology coverage
still fails closed; merely enlarging boxes is not acceptable.

**Correction/deterministic falsifier:** source semantics v13 emits every
contact-enabled active body's captured ECL/native-motion AABB trajectory from
root, not only future timeline spawns. Lease v3 accepts only that source
version and checks each fresh body at the stable observation frame; its old
exact future remains immutable. In the retained f469/f474→f475/f482 witness,
pipeline and position remained compatible with 74 frames left. The old check
passed the first three projected frames and revoked only at invented future
f478, proving the fresh observation itself was not the mismatch. New tests
retain a high fresh velocity when its current seam is contained, revoke an
already uncontained current body, and reject unstable issue snapshots.
Focused Linux gates and 149 affected Windows tests pass. Physical Stage-3
rotation is still required.

## CE-0256 — Lease v3 reached physical selection but still never governed input

**Observed physical:** source-v13/lease-v3 Stage-3 run `20260801_152531`
completed six hits at `[1255,1991,7576,8339,23938,25481]`, four ordinary and
two spell, with zero Bombs, accepted replay SHA-256
`82eb705e36ff9e3056965e58934277200ce8dc2a160d96d48386694afe130db8`,
and complete cleanup. Different RNG makes its six-versus-four v12 hit count
observational. The first hit at f1255 was an ordinary committed-prefix
collision after the pipeline margin became negative; no global gate was
available on the hit row.

**Observed mechanism:** 653/1,190 future-source roots completed; 267
prepublication and 13 delayed exact issues were effective. Ten leases were
created, three renewed, and 13 later captures observed a lease. Two reached
lease authority selection, but effective issue count remained zero. Six
capture revocations were `fresh_body_envelope_not_contained` even though each
row had zero contact-enabled bodies. Three more revocations reported
`selected_action_changed_without_predecessor`, although the issued and lease
commands had equal direction/focus after stripping `+deadline_hold` and the
SHOT bit. The remaining selected lease passed pipeline and body geometry but
was rejected by `fresh_local_lease_recertification_failed` when the exact old
proof was already the authority.

**Inferred correction:** current contact-disabled bodies are not native
contact hazards; their possible future enable is already part of source-v13
ECL trajectories. A SHOT-only auto-confirm pulse does not alter the certified
movement path, while direction/focus changes still require a new predecessor.
The old exact continuation plus stable body-seam containment is authoritative;
a second short local certificate over the same stale bullet read cannot veto
it. Preserve unknown source/topology and true uncontained contact bodies as
fail closed, then require nonzero physical lease consumption.

**Implemented deterministic falsifier:** lease v4 filters current exact
contact slabs at the native contact gate, canonicalizes decorated action
labels, treats only SHOT-bit changes as movement-equivalent, and selects a
compatible older lease when a renewal table misses its final issue-age row.
The stable contact-body seam plus immutable source-v13 proof is hard; the
fresh local certificate remains diagnostic. Tests accept a disabled body and
a SHOT-only `+deadline_hold`, while still rejecting a true direction/focus
change and an uncontained contact-enabled body. Focused Linux and Windows
lease/controller/retained-chain tests pass. Physical Stage 3 is the next
falsifier.

## CE-0257 — Lease persistence passed; full-H268 source delivery did not

**Observed physical:** source-v13/lease-v4 Stage-3 run `20260801_155718`
completed seven hits at `[1263,2136,6695,7711,13011,13860,25791]`, five
ordinary and two spell, with zero Bombs, accepted replay SHA-256
`e100140171749ef039f13634afc2df369298a967af043bc89d8490440eaf43ee`,
and complete cleanup. Different RNG prevents a causal aggregate comparison.

**Observed mechanism promotion:** one lease was created and one renewed. Of
31 captures, 30 selected the lease and 29 issues retained the exact
`down_left_fast` movement with `physical_no_write=true` and no movement or
SHOT write. Every fresh contact-body seam passed. The sole revocation was
`terminal_continuation_expired_at_issue`. Lease v4 therefore closes the
retained persistence false seams without widening direction or focus.

**Observed remaining failure:** each of the five ordinary hits had zero
effective prepublication, delayed, or lease authority in its prior 240
frames; `future_policy_unavailable` dominated. Many source projections were
only truncated complete prefixes, ending below H268 at main ECL opcode
`0x05`/`0x06` or a captured enemy in movement state 2. Before canonical hit
f1263, the first nontruncated full-H268 root appeared at f1249, leaving only
14 frames for solve/publication.

**Next falsifier:** recover shipped semantics for reached main opcode
`0x05`/`0x06` and the full captured state-2 motion root. Add exact regressions
that retain H268 while preserving UNKNOWN for incomplete native state. Then
measure full-H268 source lead and proximate exact authority; do not change
local ranking or add a stage waypoint.

## CE-0258 — Source v13 omitted main loops and captured timed-motion roots

**Observed shipped semantics:** `enemy_ecl_vm_step@0x004184B0` executes main
opcode `0x05` by resolving arg2 as an integer lvalue, decrementing it, and
branching while its post-decrement value is positive; `0x06` assigns arg1 to
the arg0 integer lvalue. Stage-3's reached instructions use captured locals
and masks `0x04`/`0x01`, matching the bounded auxiliary semantics.

`enemy_motion_update@0x00422C40` advances state 2 from displacement
`+0x2DC4`, start `+0x2DD0`, easing bits 14..16, Th08Timer fraction/current
`+0x2DE0/+0x2DE4`, and duration `+0x2DE8`. Expiry tests the current integer
and snaps to start plus displacement. The former capture mislabeled timer
base/previous `+0x2DDC` as elapsed, also prematurely ending captured state 3.

**Implemented correction:** source/projection v14 captures the complete timed
root, models the retained fraction and exact integer-expiry snap, fixes the
state-3 current timer, and lowers main `0x05`/`0x06`. Missing/nonfinite state,
invalid modes/durations, and legacy state-2/3 payloads remain fail closed;
instantaneous velocity is not used to infer hidden displacement. IDA comments
at `0x00422E80`, `0x00422F8F`, and `0x00423045` record the corrected layout.

**Deterministic falsifier:** exact unit tests cover main set/loop behavior,
fractional state-2 continuation, integer expiry, full auxiliary closure, and
missing-state rejection. Source/capture/retained/controller focused Linux
gates and 147 Windows tests pass. A fresh Stage-3 physical run must show
earlier nontruncated H268 roots and authority near ordinary pressure; otherwise
v14 is only semantic closure, not a solver improvement.

## CE-0259 — Source v14 closed its target but arrived after the first hit

**Observed physical:** source-v14/lease-v4 Stage-3 run `20260801_163434`
completed four hits at `[1253,2141,8884,16134]`, three ordinary and one spell,
with zero Bombs, accepted replay SHA-256
`0c65e8835b7cd9c6837cc0cbf37575552dc86e660d11d1e6c0cc7cc057bda80e`,
and complete cleanup. The 4/3 result versus v13's 7/5 uses different RNG and
is observational only.

**Observed semantic promotion:** complete source roots increased 645→801 and
full-H268 roots 47→58. Reached main `0x05`/`0x06` and captured movement-state-2
failure reasons disappeared, so source v14 closes its stated model mismatch.
Exact prepublication/delayed/lease authority was effective on 316/7/5 issues;
three leases were created and four renewed.

**Observed remaining counterexample:** all three ordinary hits had zero exact
authority in their prior 240-frame windows. Before canonical hit f1253, the
first full-H268 root was f1223 and was submitted at f1230. Its 2352.884 ms
solve completed at f1371, after collision and policy expiry. The added 16
frames of source lead did not cover the roughly 141-frame physical delivery
pipeline. Newly exposed blockers include main float-add `0x19`, main/auxiliary
transform definition `0x6F`, auxiliary return `0x35`, and later transform
semantics. Close only shipped exact cases; unresolved operands or call-stack
state remain UNKNOWN.

**Storage:** after compact reports and replay provenance were verified, the
accepted Stage-3 `152531` raw/log bundle was retired (350,708,909 bytes).
Its compact reports and replay remain; Stage-3 keeps the newest two accepted
replay-capable raw bundles.

## CE-0260 — Reached arithmetic, transform records, and bottom return were exact

**Observed shipped semantics:** `enemy_ecl_emit_bullets@0x00422720` evaluates
direct-fire type and color independently from the packed low/high signed-i16
operands. `enemy_ecl_vm_step@0x004184B0` implements `0x25` as in-place angle
normalization and `0x6F` as seven independently evaluated writes to one of 18
24-byte transform records. `ecl_return_subroutine@0x00421CB0` pre-decrements
signed depth: captured auxiliary depth zero terminates/reset-switches the
context, while positive depth restores a saved 0x228-byte frame.

**Implemented correction:** source v15 lowers main float `0x19/0x1A`, dynamic
fire type/color, set-valued-safe `0x25`, point-valued `0x6F`, and depth-zero
auxiliary `0x35`. A set-valued transform field, positive saved-stack depth,
or out-of-range record stays UNKNOWN. Active vector/angular acceleration,
stop/re-aim, and reflection use a conservative full-disc path bound; template
replacement expands collision geometry to every reached target template.

**Deterministic falsifier:** Stage-3 sub0's actual time-65 sequence now closes
through its arithmetic, transform definitions, and six difficulty-eligible
fires, including active `0x20` geometry. Focused source/birth/projection/
capture/retained/predecessor/lease/controller tests pass 93/93 on Linux and
Windows. A physical Stage-3 repeat must move full-H268 coverage early enough
to survive the measured publication latency; otherwise this is only another
exact semantic closure.

## CE-0261 — Finite exact authority expired inside an unguarded synchronous scan

**Observed physical:** source-v15/lease-v4 Stage-3 run `20260801_171249`
completed ten hits at
`[594,946,2260,3838,7959,8973,14085,15363,23042,26069]`, eight ordinary and
two spell, with zero Bombs, accepted replay SHA-256
`4af27ae69259182ed580f1b764cbd131583abacd266b31580c544ef0c95db5aa`,
and complete cleanup. The different-RNG 10/8 total is observational.

**Observed source promotion:** 519/1,065 source roots completed, 436 were
truncated, and 83 were full H268. The v15-targeted arithmetic, dynamic fire,
return, transform-write, and supported-transform failure reasons disappeared.
The canonical f354..594 window contained 13 full roots and exact
prepublication authority on 43/50 decisions. This falsifies source absence as
the sole first-hit cause.

**Observed causal counterexample:** f565 retained exact `down_left` authority
only through terminal f571 and created no continuation lease. The next root
was captured at f566. Its synchronous fallback evaluated `up_left`,
`down_left`, and `left_fast` constant-H80 tables for 394.522 ms; the entire
issue path took 506.576 ms and did not commit until f594, 28 frames later,
when native `phase_at_action` was already 2. Bullet slot 1 was present in the
f566 pool. Under the observed focused `down_left` movement it has signed AABB
clearance +1.972 at f586, -0.432 at f587, -1.742 at f588, and -2.553 at f589.
The hit is therefore not a sensor gap: the finite proof expired while the
solver synchronously held its input.

**Inferred contract failure:** the delayed table may create a continuation
lease after it finishes, but its computation is itself an unproved no-write
hold when no old lease exists. Also, the three-action terminal probe spent
two slots on fixed `left_fast/right_fast` while the current query already
reported `up_left` as a high-repair viable alternative. Candidate ordering is
not authority, but omitting that direction forced the expensive fallback.

**Implemented correction:** a synchronous delayed scan now requires either a
compatible continuation lease or an exact constant-held-action certificate
covering its complete effective issue-age/pickup support. Unsafe or unavailable
held geometry blocks the scan without granting an action. The terminal probe
orders current-kernel repair-volume candidates after the held action but still
intersects each with its independent exact prefix and terminal predecessor.
The retained f566 slot-1 root fails the held guard; its probe order changes to
`down_left/up_left/down`. Focused Linux and Windows tests pass 33/33.

**Next physical gate:** rotated Stage 4A must report zero long scans whose computation
guard failed, must preserve hard no-Bomb, and must not repeat a canonical hit
caused by an unguarded finite-certificate expiry. Probe suggestions must not
widen an allowed set from shadow guidance alone.

## CE-0262 — Hard spell policies lost action authority after sound scale rejection

**Observed physical:** Hard Stage-4A run `20260801_175112` completed with 11
hits and zero Bombs. Eight hits were in spells and three in nonspells. The
trace had 4,150 queryable spell-policy rows but zero constrained spell
decisions. At spell-56 frame 11397 the corridor had a nonempty viability set,
while `corridor.action_authority=false` and
`time_scale.hard_authority=false`; the provenance was
`diagnostic_constant_current_root_unknown_direction_no_authority`.

**Observed historical contrast:** different-RNG Hard Stage-4A run
`20260726_212756` at code checkpoint `709e858` constrained 8,073 decisions,
including 967/936/170/778/506 across spells 56/60/64/68/72. It selected 725
recovery and 2,845 distant-recovery rows, with decision cadence median/p95
2/3 and local planning p95/max 20.830/38.136 ms. Current `175112` constrained
only 847 ordinary decisions, selected zero recovery rows, had cadence 3/4,
and local p95/max 32.725/250.616 ms. Its 10/11 hits were boundary-associated.
The aggregate 6-versus-11 hit count is observational; the authority and
delivery differences are direct trace mechanisms.

**Observed native/static cause:** SEM-SCALE checkpoint `6a71ac1` correctly
rejected repeating a root scale because enemy ECL runs between player and
laser updates. Shipped dataflow identifies callbacks 18/28/29 as gameplay
scale writers; other write sites are initialization resets. Exact decoded
ECL for Stages 1–5 has no dynamic callback index and no invoke/install of
18/28/29. Stage 6 and Final contain callback-18 writes.

**Inferred correction:** do not restore the old root-repeat assumption. Add a
Stage 1–5 constant-unit schedule certificate bound to exact runtime/static ECL
identity, coherent route/difficulty/stage/root state, and complete active
installed-callback inventory. Fail closed on any mismatch. Its physical gate
is nonzero spell action authority and constrained spell decisions on Hard
Stage 4A; different-RNG hit total alone cannot promote it.

## CE-0263 — Exact scale activation exposed coarse relaxation and cell collapse

**Observed physical:** Hard Stage-4A `20260801_184903` bound the exact
runtime/static ECL identity and complete installed-callback inventory. It
constrained 3,599 decisions after the configuration-invalid `183730` run had
zero, including 2,678 constraints across spells 56/60/64/68/72. Recovery and
distant-recovery were selected 319 and 809 times. It completed 12 hits, first
hit 1209, and zero Bombs. This passes CE-0262's schedule-authority mechanism;
the different-RNG hit total is observational.

**Observed contract failure:** `184903` allowed 57 coarse terminal-threat
relaxations despite naming exact corridor authority. Exact corridor and every
ordinary exact authority are now non-relaxable. Follow-up `191508` recorded
zero relaxations.

**Observed continuous-position falsifier:** the old 16px corridor required
zero extra clearance and therefore certified only lattice centers. Consuming
the full cell half-diagonal made the hard set a continuous-position lower
bound. In `191508`, query count rose 5,104→5,775 and median solve latency fell
131.5→78.7 ms, but empty queried sets rose 1,723→2,927, constrained decisions
fell 3,599→3,001, and the different-RNG run completed 18 hits with 12 boundary-
associated contacts and zero Bombs.

**Inferred conclusion:** missing spell authority was real and is repaired,
but uniform coarse-cell erosion is not a viable global solver. Do not restore
center-only hard authority or coarse relaxation. A future TH08 successor must
use adaptive/local exact continuous-position refinement and show nonempty
pressure-window authority before physical promotion.

## CE-0264 — The Wine “full-unlock” score exposed only Stage 1 for the selected team

**2026-08-24; observed physical bootstrap, correction tested offline.** Wine
Practice Stage 5 attempt `20260824_114355` verified the exact EXE,
no-life-decrement patch, Lunatic cursor, Route 2, and Sakuya/Remilia, then
failed closed before gameplay because the native availability mask was
`0x0001`. The raw physical CRLF session SHA-256 is
`8a6abb88f640cef98bd0bb72673006f622d696b163f34e0509a20459969d4c1e`;
the tracked canonical-LF blob is
`9dcd42ab09a1bba70aac9504ddff7fc0d41c9b7d3ad00ade2788ad0395a35fc2`.

The invalid assumption was that the supplied file label “full unlock” implied
all per-team/per-difficulty Practice clear records. Authoritative
`TitleScreen::OnUpdatePracticeStageSelect` instead reads only
`clrdData[shotType].difficultiesClearedWithRetries[difficulty]`; zero is
special-cased to Stage 1. The observed `0x0001` is therefore a score-state
fact, not Wine input loss.

The correction is explicit and fail-closed: after exact stage-menu, Route-2,
and difficulty verification, a Practice-only opt-in ORs only the requested
stage bit into the source-defined `u16` data field, rereads it, and records the
address/before/after values. It does not patch the EXE or alter gameplay
resources. A deterministic regression proves Stage 5 maps `0x0001→0x0021`
with one `u16` write. Physical retry remains required.

## CE-0265 — Wine Practice entered Stage 5 then stopped on unknown scale authority

**2026-08-24; observed physical bootstrap, explicit diagnostic wiring tested
offline.** Retry `20260824_115523` physically verified the source-bounded
Practice unlock (`0x0164BA7C: 0x0001→0x0021`), selected cursor/stage 5, and
armed the no-Bomb agent. At frame 1 it deliberately emitted
`time_scale_authority_unknown` and terminated with no decision records. The
raw session SHA-256 is
`4903315b08076e075b0ef41a317cb0c36f504d7dd23d726e0015828d790a22c4`;
the tracked canonical-LF blob is
`409f286e5f3382e8dfb27b02614db4626c427de99e76d761b37ac7d36817ee75`.

The invalid infrastructure assumption was that a local-only Practice gate
could omit both exact ECL scale authority and the existing diagnostic
continuation. Strict fail-closed behavior was correct. The Wine runner now
exposes `--diagnostic-continue-root-only-scale` only for Practice and records
it; default Practice remains fail-closed. This unknown-direction constant-root
proxy may validate local/issue latency and lifecycle only. It cannot promote
source-global authority or exact scaled geometry. Focused physical retry is
still required.

## CE-0266 — Exact lazy issue work survives physically but spell 107 falls back

**2026-08-24; accepted physical mechanism gate, survival not promoted.**
Stage-5 Practice run `lunatic_route2_stage5_unattended_20260824_115819`
completed frames 2..45,254 with 7,792 decisions, 28 native hit edges, unchanged
Bomb stock, no Bomb action/flag/mask, the no-life-decrement patch, exact
post-stage no-save input, and exact-prefix cleanup. The ignored raw trace is
426,371,177 bytes with SHA-256
`e5a7eecb11a25b53ae8415087a84d0f044a4b37d15e0978800915a829199a348`.

The lazy mechanism is present: 2,644/2,995 changed-prefix transactions ended
after an exact selected-action proof at `3.970/6.992ms` median/p95. The
remaining 351 exact full fallbacks took `62.454/76.416ms`; spell 107 alone
caused 300, with median 81 pipeline branches and 12 observed hits. Its
decision cadence reached 13 frames p95, outside the modeled delay support.
This falsifies the assumption that optimizing the common safe-selection path
alone bounds issue latency. It does not justify weakening exact fallback or
adding a spell-specific policy; first share/bound the generic all-action work.

The first hit at frame 4,163 is a source-AABB bullet overlap at the bottom
boundary. Robust viability became empty ten frames earlier and global
viability 237 frames earlier, while issue lag four remained inside support.
It is therefore a policy/escape counterexample distinct from the later
latency cascade. Practice's full-Power and diagnostic scale proxy make the
aggregate hit comparison observational.

## CE-0267 — A combined sensor window rewound old bullets from a newer player

**2026-08-26; physically observed, source-confirmed, fixed offline.**  Easy
Final-B hits 11417 and 53738 were attributed to a sensing gap, but retained
slots 1156 and 340 become native state 5 at exactly the source-predicted
contact coordinates.  The second bullet pool is point-captured at frame 53735
while the stable player root and unrelated boss/ECL reads are frame 53736.
The combined window therefore reports age zero even though the causal bullet
is one update old.  Local certificates then negate a source-to-hazard offset
and under-advance both bullets.

The live path now retains the bullet bracket's complete discrete age support
relative to the final player root and evaluates every supported trajectory.
Exact regressions change the two causal slots from `0/+4.157` and `0/+2.267`
collision/clearance to `1/-1.224` and `1/-0.214`.  At the preceding frames
11412 and 53730, the correction rejects the issued action while alternatives
still exist.  This is a generic clock fix, not a spell/slot patch.  A fresh
physical Practice run remains required; laser and async enemy epochs remain
open.

The physical falsifier `easy_route2_stage6b_unattended_20260826_063823`
contains no residual sensing-gap classification, but completes with 11 hits
rather than improving the nine-hit checkpoint.  Treat that as observability
validation only; AUD-094/CE-0268 track the policy and aggregation defects.

## CE-0268 — Correct bullet ages exposed a dormant local horizon and duplicate-risk aggregation

**2026-08-26; accepted physical falsifier, generic root causes confirmed.**
Easy Final-B run `easy_route2_stage6b_unattended_20260826_063823` completed
13,083 decisions over frames 2..74364 with 11 hits, no Bomb input, normal
route completion, and clean prefix teardown.  All contacts now have observed
or modeled geometry, but ten exhaust the short robust action set before the
hit.  Six are boundary-associated and warning leads reach 48 frames.

The live planner nevertheless records no terminal warning because the
configured 32-frame threat horizon is reduced to the 10-frame beam whenever
global guidance is absent.  Separately, a two-age bulk read is concatenated
as two simultaneous copies of every slot.  That preserves a conservative
zero-collision test but double-counts mutually exclusive risk and raises
local-plan median/p95 from 31.23/43.81 ms on singleton reads to 42.25/72.06
ms on two-age reads.  The next offline gate must aggregate uncertainty per
slot and then activate the cheap local tail; it must not add a spell branch or
use global authority.

The first half is fixed offline: one outward-rounded AABB hull per native slot
contains every supported age and is lethal if any supported age is lethal.
It retains the two exact physical collisions while reducing a deterministic
400-bullet full local pass from 26.17/27.16 to 22.75/23.61 ms median/p95.
The complete suite and a 360-check atomic/straddled stateful geometry gate
pass.  The dormant 10..32 local tail remains the next independent experiment.

## Next ID

Use CE-0269. Keep new entries compact and retain the smallest deterministic
evidence.
