# TH08 Research Log — 2026-08-25

## Retained-root capture exposed a Practice/Spell-Practice identity error

- **Attempt:** `lunatic_route2_stage5_unattended_20260825_010248` ran in the
  dedicated TH08 Wine prefix/display with Lunatic Sakuya/Remilia Stage 5,
  hard no-Bomb, capture-only future-source retention, and nonbinding
  86,400/86,700-second agent/trial limits.
- **Physical observation:** at gameplay frame 0 the relocated ECL image was
  47,224 bytes and normalized to SHA-256 `3148f45f...`, exactly decoded
  `ecldata5.ecl`. The command had incorrectly pinned 31,184-byte
  `ecldata5sp.ecl` SHA-256 `d9140821...`. Exact identity failed closed, so the
  trial produced no decisions and no retained producer root. It stopped in
  about 17 seconds because of identity, not duration; exact-prefix cleanup
  reported no leftovers.
- **Source root cause:** `GameManagerFlags` names bit 0 `isPracticeMode` and
  bit 14 `isSpellPractice`. `EnemyManager::AddedCallback` branches on bit 14,
  not bit 0. Practice Start uses `g_StageEclFiles` just like route play;
  stage-scoped Spell Practice below card 205 uses `g_StageSpellEclFiles`.
- **Correction:** Practice Start identities now equal the route identities.
  The former `*sp.ecl` table is retained under the explicit
  `STAGE_SPELL_PRACTICE_ECL_IDENTITIES` name for static cross-package producer
  checks. The corrected producer contract is schema v2 and no longer calls
  `*sp.ecl` a Practice Start image.
- **Verification:** 56 focused tests pass. The next action is the same isolated
  root capture with the corrected `ecldata5.ecl` identity; no algorithmic or
  hit-count claim follows from this infrastructure repair.

## Corrected Stage-5 acquisition retained four physical roots

- **Accepted capture:** `lunatic_route2_stage5_unattended_20260825_011207`
  reached `route_complete` at manager frame 43,173 from clean commit
  `b771d31`. It used display `:98`, CPUs `24-47`, hard no-Bomb, exact
  `ecldata5.ecl` identity, complete no-scale-writer authority, and the
  86,400/86,700-second outer limits. Exact-prefix leftovers were empty. The
  raw trace SHA-256 is `c8a35929...`; raw data remains ignored.
- **Corpus:** content-addressed capsules were written for the four cards this
  route physically visits: 103, 107, 111, and 115. Card 118 did not occur.
  The phase-0 roots for 103 and 115 stop at a singleton auxiliary callback and
  unknown type-16 template geometry, respectively. They contain the full
  coherent payload needed to repair those semantics offline.
- **Transition bug:** the 107 and 111 workers completed in player phase 3.
  Their stale active spell IDs still matched the filter, so the old code wrote
  them and consumed the one-root quota. They are valid physical transition
  states for the fuzzer but invalid planner roots.
- **Correction:** retention jobs now bind route, difficulty, stage, spell,
  player phase 0, and bomb-inactive state at submission, then recheck the
  captured root before writing. A phase/context mismatch records a reason and
  consumes no quota. The live submit gate also requires phase 0.
- **Interpretation:** the run recorded 22 hits, but this is not an algorithmic
  comparison: capture added observer work and retained projections remained
  shadow-only with zero new action authority.

## Generic retained-root event stream is connected fail-closed

- **Architecture gap:** retained capsules terminated at
  `FutureDirectFire`, while the complete-stage laboratory began at resolved
  `BulletEmitter` events. There was no checked bridge, and an UNKNOWN slab's
  requested horizon could be mistaken for proven coverage.
- **Correction:** a content-addressed importer now verifies the exact ECL
  image and coherent planner-root predicate, reruns source closure, and maps
  only point-valued direct-fire events to one-shot emitters. It preserves the
  event's resolved player-aim angle exactly. Intervals are never replaced by
  midpoints; flags, lifecycle/ANM state, transform programs, and callbacks
  remain explicit rejection boundaries.
- **Physical corpus result:** the phase-3 roots are rejected before replay;
  the singleton-callback root has no proven prefix; the template-geometry root
  yields an empty 86-frame causal prefix and then stops before unknown type-16
  geometry. The adapter is therefore physically exercised but has not yet
  imported a hostile birth or changed a planner outcome.
- **Generality:** production lowering contains no stage/spell identifier
  branch. Card identifiers occur only in corpus assertions. The next semantic
  checkpoint is generic source-derived ANM/lifecycle and template geometry.

## Material artifact cleanup

- `artifacts/runtime_reports` was first reduced from about 15 GB to 2.6 GB,
  then the final artifact audit reduced the complete `artifacts/` tree to
  about 130 MB. Every removed raw trace had compact session, summary, dossier,
  regression, comparison, death/collision, and run-note evidence retained.
- Removed ignored raw JSONL for complete routes
  `20260823_170206`, `20260823_183138`, `20260824_022909`,
  `20260824_034510`, `20260824_051944`, `20260824_094658`, and Stage-5
  `20260824_115819`. These local raw streams are not recoverable from this
  repository; their compact Git evidence remains.
- The last cleanup removed the 2.22 GB 57-hit route raw stream
  `20260824_074407` and the 418 MB physical-root Stage-5 raw stream
  `20260825_011207` after confirming that neither was tracked, open, or
  referenced by tests/docs. Their compact reports and the four small
  content-addressed root capsules remain. No tracked artifact was deleted;
  the removed local raw streams are recoverable only from an external backup
  or by rerunning the corresponding acquisition.

## Source/asset template contract removed a false geometry boundary

- **Confirmed bug:** native capture retained 16 bullet-template rows although
  `BulletManager::AddedCallback` initializes exactly 21. The descriptor type
  indexes this table directly. This was a generic observer defect, not a
  card-specific missing rule.
- **Exact contract:** source commit `57ee34f45e...` and decoded `etama.anm`
  SHA-256 `c3d19370...`/613,780 bytes regenerate all 21 collision extents and
  lifecycle-script terminal ages. The shipped asset was parsed directly in
  memory; no decoded ANM or other large artifact was written.
- **Correction:** future captures read all 21 rows. An old 16-row root can use
  the missing static suffix only after its complete observed 0--15 prefix and
  native metadata equal the pinned contract. Altered/sparse/duplicate evidence
  remains UNKNOWN.
- **Same-root result:** the retained phase-0 type-16 root still proves 86
  frames, but its frame-87 boundary advances from missing geometry to the
  genuine callback-14-family flag `0x100000`. It still produces zero births,
  so this is not a planner or hit-count improvement.
- **Next confirmed bug:** the current future-birth envelope hardcodes every
  state-2 ANM completion to age 10. The exact generic per-type ages are 10,
  24, or 30; states 3/4 differ as well. The contract retains those facts, but
  integration waits until `FutureDirectFire` carries bullet type and the
  same-update activation order is modeled exactly.
- **Verification:** 70 focused tests and the complete 1,455-test Linux suite
  pass with five conditional skips and zero failures. The English paper also
  compiles successfully. No Wine trial was run.

## Generic ANM spawn lifecycle is closed through future geometry

- **Source/asset correction:** `FutureDirectFire` now carries native bullet
  type. One generic selector implements the source flag priority
  `0x02 -> 0x04 -> 0x08`, the state-2/3/4 divisors `2/2.5/3`, the 21-row
  asset terminal-age profiles, and the source's same-manager-call transition
  into full state-1 motion. No production branch references a stage or spell.
- **Lethal semantics:** future bullets are withheld from AABB and sector
  hazard fields while their spawn ANM state is nonlethal and appear on the
  exact terminal update. Type, lifecycle flags, and causal reconstruction now
  survive every event-IR boundary. Unknown callback tag `0x100000` still
  fails closed, so the retained spell-115 root remains an empty 86-frame
  prefix ending at the callback-14 boundary.
- **Independent oracle:** the tracked C transcription covers all 21 bullet
  types, no lifecycle, each singleton lifecycle flag, and mixed flag classes
  that exercise native priority. Samples straddle every terminal age and use
  independent C binary32 position updates. Discrete state, lethal activation,
  AABB containment, and compact-sector uncertainty all agree.
- **Second bug exposed by the differential:** the former ideal-expression
  geometry plus fixed `2e-5` guard failed on 392 of 420 lethal samples; the
  maximum axis error was `8.17512e-4`. The replacement outward-rounds an
  interval after every source-order binary32 scale, divide, and add for AABB
  geometry. Sector lowering preserves the shared speed/angle parameter and
  analytically propagates an explicit error term for every native operation,
  including a speed-scaled initial trig-component bound. An additional 256
  random mode-8 roots at three lifecycle ages are all contained by the compact
  sector.
- **Capacity/performance:** the native-pool stress lowers 1,536 births for 269
  frames with finite output. Eliminating per-bullet sector interval histories
  reduced the joint 16/269-frame benchmark from 4.84 s to 0.61 s (about 7.9x)
  at about 67 MiB peak RSS; it wrote no report artifact.
- **Verification:** the complete Linux suite now passes 1,463 tests with five
  conditional skips and zero failures. No Wine or route run was performed,
  and no hit-count improvement is claimed. The next generic producer boundary
  is child/timeline births followed by callback 14, before global-planner
  shadow integration.

## Lifecycle operand is now part of the global projection identity

- **Post-test identity audit:** `bullet_type` affected the new lifecycle
  geometry and survived causal event reconstruction, but the canonical future
  projection payload omitted it. A flag-free type-2 event could therefore
  alias a type-7 event when their explicitly supplied geometry was identical,
  despite those types selecting different terminal ages under state-2/3/4.
- **Correction:** future-hazard projection schema/version v6 includes the type
  in its digest. A regression first proves equal flag-free trajectories but
  unequal digest/version identities, then proves that enabling state-2 yields
  different lethal activation frames. This closes a generic global-cache join
  bug without granting action authority.

## Native spawn lifecycle now executes through complete-stage histories

- **Coverage gap closed:** the source-exact lifecycle existed in the bounded
  future lowerer but not in `StageProgram` or its stateful runtime. Schema v3
  now carries the canonical native type/flag pair while preserving exact v1/v2
  replay identities. Malformed pairs, unsupported flags, and schema downgrades
  fail closed.
- **State separation:** native state-2/3/4 and its timer are not callback-12's
  phase/auxiliary state. The runtime stores them independently, applies the
  four-velocity initial offset and divided pre-activation motion, suppresses
  culling/collision until state 1, then performs activation, full movement,
  and collision in the same terminal update.
- **Generic boundary:** the unbounded generator spans all 21 source templates
  and all three lifecycle flag classes. Lifecycle+callback/transform
  composition remains rejected pending source-order closure. No stage/spell
  identifier participates in generation, execution, or selection.
- **Long differential:** seed `0xce0132`, profile `gate`, completed all 3,600
  frames with 57,871 allocated births, 132,455 finite-pool suppressions, 85
  saturation frames, 18,665 lifecycle activations, and 120 local-planner calls.
  The independent C oracle checked 1,412,926 reached lifecycle samples with
  zero state/position mismatch; RNG, collision membership, hard no-Bomb, and
  geometry gates passed. The synthetic normalized collision count (152) is
  not a route hit metric.
- **Artifact policy:** the complete 103,397-byte report had SHA-256
  `16176920feeee82024d81d0ee96acc99b533beaaf8119124d737ad7322c994de`.
  Its metrics/hash are recorded here and the temporary file was removed; the
  compact tracked v1 report remains as a backward-compatibility fixture. Two
  ignored early-Stage-1 raw traces (`20260823_165837`, 41,760,039 bytes, and
  `20260824_122321`, 33,019,124 bytes) were also removed after confirming that
  neither was referenced and both were superseded by later complete-route
  dossiers. Their small launch logs and the available summary were retained;
  `artifacts/` fell from about 130 MB to 59 MB, with no remaining file above
  5 MB.
- **Next:** import child/timeline births into the same event stream, then close
  lifecycle composition and callback 14 before global-planner shadow use.

## Local planner lifecycle shortcut is removed

- **New root cause from stateful integration:** the real local hazard projector
  still modeled only state 2 with the type-0-class timer 9, advanced states
  3/4 as ordinary full-speed bullets, and filtered only state 5. It therefore
  created immediate false lethal walls for all spawn states and used wrong
  future positions/activation ages for several native template classes.
- **Zero-read type recovery:** `AnmVm.scriptIndex` at bullet `+0x21a` is in the
  existing pool slab. The 21 source-initialized normal scripts are unique, so
  sparse and packed decoders recover exact type without another RPM call or a
  stage/spell rule. Trace lifecycle schema v2 retains the type; v1 is readable
  but cannot authorize an exact preactivation replay.
- **Correction:** local collision semantics v3 selects the type/state divisor
  and terminal age, preserves binary32 divided-plus-full terminal order, and
  admits only state-1/aux-0 bullets to the lethal kernel. Missing type and
  impossible timers fail closed, and the version join invalidates older cached
  solutions.
- **Differential:** all 21 types and state 2/3/4 classes were started at three
  preterminal ages and advanced four frames. Position, native state, and lethal
  membership matched the independent C oracle exactly. Both raw-pool decoder
  paths recover all 21 types.
- **Outcome falsifier retained:** the post-fix 3,600-frame gate kept the same
  final player position and 152 synthetic normalized collisions. Its planner
  median/p95 was 22.37/27.28 ms versus the earlier 21.21/27.49 ms. This single
  gate proves neither latency nor survival improvement, so no Wine run follows.
- **Verification:** the complete repository suite passes 1,474 tests with five
  conditional skips and zero failures/errors. The legacy root2129 audit now
  explicitly labels its missing-type lifecycle comparison as a historical
  current-state lower bound instead of defaulting the absent type.
