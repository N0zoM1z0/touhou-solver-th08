# Research Log — 2026-08-26

## Complete Easy baseline recovered after the interrupted work session

- The clean-checkpoint physical trial
  `easy_route2_fullrun_unattended_20260825_101715` completed the entire
  Sakuya/Remilia Route-2 Easy path under the no-life-decrement patch and hard
  no-Bomb input contract. It ran 49,325 decisions over frames 1..213262 and
  terminated with `route_complete`; the isolated prefix left no processes.
- Native hits were 13: Stage 1/2/3 `0/0/0`, Stage 4A `1`, Stage 5 `1`, and
  Final B `11`. Exact hit frames, spell attribution, geometry witnesses, and
  latency fields are retained in the compact dossier and generated run note.
- The corrected Final-B source service acquired hard authority on Easy spell
  187 and delivered the complete 300-frame callback-18 time-scale schedule.
  This physically closes AUD-083's difficulty/stage identity bug.

## Evidence changes the next implementation order

- Frame 67903 is a local terminal-position trap: the player reached the bottom
  boundary before the certified action set exhausted. Eleven of 13 route hits
  are boundary-associated, but any correction must optimize generic escape
  width/viable continuation inside existing collision safety, not dispatch on
  a stage or spell ID.
- Frame 139187 is a callback-14 visibility failure: 437 nearby bullets were
  excluded while callback-aux suppressed at frame 139182 and became lethal
  before a usable input response. The source orders `FUN_00424c40` before
  bullet motion. This exact transition is the first new long-stage fuzzer
  workload and C differential.
- The route exercised no global solver at all. `ordinary_preexhaustion` was
  explicitly disabled, yielding zero queries and zero constrained decisions.
  This separates a configuration/identity failure from planner performance.
- The VPS now exposes CPUs 0..15 and 64 GiB RAM. The Wine wrapper's historical
  default `24-47` was invalid on this host. Its new `auto` mode derives the
  upper half of the effective process affinity (`8-15` here), while preserving
  an explicit taskset expression and recording requested/resolved values.

No new Wine run starts from this checkpoint. The next gates are callback-14
complete-stage replay against the tracked C oracle, long transition stress,
and a generic boundary-continuation regression; only then is Easy Final-B
Practice worth the physical latency cost.

## Callback-14 complete-stage gate closed

- The stateful stage format now carries callback-12 and callback-14 events in
  one source-ordered stream. Callback 14 uses the same per-bullet phase and
  auxiliary state as the executable and runs before movement/collision.
- A fixed regression reproduces the complete five-frame state sequence
  `(phase, aux) = (0,0), (1,0), (0,1), (2,1), (1,0)` and proves that the final
  callback-14 reactivation occurs before motion and is collision-eligible in
  that same update. Python and the retained C oracle agree.
- The 3,600-frame seed-`0x8414` gate reached the real 1,536-slot pool limit,
  applied 28,064 callback-14 changes, and produced 5,212 suppressed-to-lethal
  reactivations, two of which collided in the transition frame. All 1,220,560
  lifecycle samples matched exactly and the full source differential passed.
- This does not yet fix policy. It removes a modeling ambiguity: the Stage-5
  root cause is now future-event delivery into the local planner, while the
  generic callback transition kernel itself is independently closed.

## Stage-4 beam starvation localized and corrected offline

- Same-root action partitions prove that the Stage-4A failure was not merely
  a weak boundary penalty. At frame 67882, width 24 discarded every useful
  `left_fast` continuation and chose `down_right` at 1.60 px clearance;
  width 256 chose `left_fast` at 8.35 px. At frame 67885 the historical
  `right` path was already negative by 6.00 px while forced `left_fast`
  remained positive by 4.40 px.
- Preserving the first-action label during deduplication alone was
  insufficient because global top-k still evicted the action family. The new
  reducer keeps one leader per first action only inside the best hard class
  and only when the root is within one action-hold displacement of a boundary.
  Interior planning retains historical global reduction.
- The gated width-24 reducer now matches width 256 at both witnesses in
  18--22 ms instead of 84--97 ms. A new native v2 ABI carries the explicit
  gate; v1 remains behaviorally compatible. The rebuilt Win32 DLL digest is
  pinned by the isolated runner.
- On 96 deterministic full-route roots, five actions changed and all five
  retained the identical all-zero hard vector. The two reported hard-vector
  differences were same-action replay diagnostics, not action regressions.
  The compact audit SHA-256 is
  `9157ec2ddbd79f414a2c3d0131a02becba118885a9a22716f949c15bd8c65228`.

## The fuzzer exposed a future-delivery confounder

- Eight fixed quick seeds passed all semantic, geometry, lifecycle, and hard
  no-Bomb checks. Historical width 24 recorded one normalized hit;
  unconditional action strata recorded seven; the boundary-gated reducer
  recorded three. The gated report SHA-256 is
  `2000f0c4feee6f4d7a4f4fe05195ab6a926ceeb00754dc4bdfbdd225b125c173`.
- The first gated divergence on seed `0x841401` is locally safer, not less
  safe: at the bottom edge it replaces a 1.98 px continuation with a 6.85 px
  continuation. Later births cause two normalized hits because the closed-loop
  campaign passes only the delayed current snapshot to the solver even though
  its `StageRuntime` owns the exact future event stream.
- A width-256 control supports that diagnosis: historical width 256 also
  diverges and records two hits, while the new width 256 records one. These
  counts cannot rank pruning quality until current and source-known future
  hazards are joined at one clock. That local future join is now the next
  offline implementation gate; no Wine run is justified by this checkpoint.

## Exact-clock resolved-stage future join implemented

- The campaign now lowers every due resolved direct-fire event, callback 12/14,
  and laser birth from the same sensed root.  Existing-pool callbacks are
  composed at that root, strictly later callbacks remain on future births, and
  active callback/transform interactions use a conservative finite AABB
  fallback.  The projection is consumed by the control prefix, hard preflight,
  beam, terminal threat score, and final recertification.
- The first integrated seed exposed unsupported stop/snap transforms; source
  review generalized the future envelope to deceleration and all three stop
  variants.  A later audit caught the important source constant: transform 1
  begins at speed 5 rather than the descriptor speed.  Its future and current
  fallback bounds now include that maximum.
- Two campaign timing bugs were causal.  Delayed hazards had been paired with
  the current player and charged snapshot lag again.  Separately, a two-frame
  beam hold invented command changes while the executor reused the selected
  action for the full eight-frame planner cadence.  Coherent queued roots,
  single elapsed control delay, and `effective_hold >= planner_stride` remove
  both shortcuts.  Enabled future coverage is now also a hard pass condition.
- With the corrected realistic two-frame cadence, fixed seeds
  `0x841400..0x841407` complete 3,840 frames with zero normalized hits and zero
  collision frames in both future-aware and blind modes.  Future-aware closes
  1,920/1,920 joins and exercises 113,119 callback/transform fallbacks.  Its
  median case p95 is 77.08 ms versus 17.75 ms blind, so the object-heavy path
  is offline authority, not live-ready code.  Report SHA-256 values are
  `a64b8b9756e1dffcc95c11751d41fd6dfe4491b4695277264611e7cc3f24f001`
  (future) and
  `639b5067b5b5752b6d534b9985ff7c8706fb444ea823988d0b46f2cb1e94d224`
  (blind); the files remain temporary and are not tracked.

This closes the experimental confounder but does not show a hit reduction on
the easy smoke corpus.  The next useful result must come from a denser matched-
cadence offline gate or a packed representation; no Wine run was performed.

## Full-suite stale expectations reconciled

- The first 1,564-test run found four failures.  All four reproduced unchanged
  in a detached clean `12f151a` worktree, proving they predated the future join.
- Three fixtures pinned pre-boundary-stratification actions/scalars even though
  their zero-collision, relaxation, repair-volume, and reserve properties still
  held.  They now assert the post-`12f151a` semantic invariants.  The fourth
  regex now includes callback-14 in the combined old-schema feature gate.
- This checkpoint changes tests only; no solver behavior was altered to make
  the suite green.

## Dense future gate and callback-time correction

- A 3,600-frame fixed gate (`seed=0x8414`, planner stride 2, width 8) finally
  separates the models: blind records five normalized hits and six collision
  frames, while the complete future join records two and three respectively.
  The temporary report SHA-256 values are
  `acedc3dea009c52d6c8c839ff4d2b176d0ccd4ebe2dd77608a87381012585b28`
  (blind) and
  `38f6cfc519a03d6b378eeaf16745c93c746dc046d1d8ddba68a3612cbfa5bdee`
  (future).  The future p95 is 360.23 ms, so this remains offline authority.
- Increasing width from 8 to 24 does not remove the two hits and expands their
  collision duration to eleven frames.  Its report SHA-256 is
  `73d5b79c7518c094d17c42c49b6938b36400e9a896eda90b90f4c83231bdd956`.
  More local candidates alone are therefore not the breakthrough.
- The wider trace found a real temporal bug: when the source callback at frame
  1740 first entered the rolling horizon, the conservative transform fallback
  applied its maximum speed from projected frame one.  Source order permits
  that speed only at the callback update.  The fallback now accumulates a
  piecewise per-frame speed bound with each callback starting at its certified
  relative frame, covered by a delayed-speed regression.
- The corrected width-8 rerun records three normalized hits and four collision
  frames (report SHA-256
  `b99c9d714c7def80a67df8e62c3b0897e2073cbe7d0b5103193af04b629ff990`).
  It is still better than blind but worse than the old two-hit path: the false
  early over-approximation was accidentally protective.  This correction is
  retained for source fidelity, not advertised as a policy gain.  Residuals
  become losing within the local rolling horizon, motivating exact
  callback/transform rollout and backward/global viability rather than another
  blind beam-width increase.  No Wine run was performed.

## Easy Final-B physical baseline exposes wasted global work

- Isolated Wine Practice at commit `01a8b66` completed Easy Final B from frame
  2 through 75551 with 11,163 decisions, 18 native hit edges, zero Bomb input,
  and accepted native terminal unload.  Hits concentrate at the playfield
  boundary: 15 carry the explicit boundary factor, 13 exceed modeled action
  lag, 12 miss a corridor deadline, and 9 use fast mode.
- Although the host runner requested authority-only global work, Practice had
  dropped that option.  It submitted 10,783 non-authoritative queries and
  completed 1,927 solves at 276.70/598.53 ms median/p95 without constraining a
  single decision.  The missing parser/session/agent wiring is fixed and
  pushed in `c4d264a`; the global framework remains available when an exact
  schedule can actually authorize it.
- The exact-scale context mismatch begins only after the final gameplay
  decision and is followed one frame later by `terminal_unload`.  This is the
  intended fail-closed terminal transition, not an incomplete Practice clear.

## Delay-scaled boundary reserve no longer needs global guidance

- The local planner previously set its control-distance reserve to zero when
  global recovery/repair/survival maps were absent.  That couples an actuator
  fact to optional long-horizon advice: with the measured six-frame delay the
  controller needs roughly 27.6 pixels to reverse, while the residual movement
  penalty begins only 12 pixels from a boundary.
- `ac77c3f` makes the existing recovery-control option always use the measured
  delay distance, while leaving diagnostic losing-state semantics separately
  gated.  It is a soft rank below collision and negative-clearance classes and
  contains no difficulty, stage, or spell branch.
- Same-root replay changes frame 10772 from `right` to `left` and frame 10778
  from `stay` to `up_left` without changing their all-zero hard vectors.  At
  frame 10792 local clearance improves from -1.10 to +4.62 pixels without a
  robust collision regression.  This is causal offline evidence for the next
  physical A/B, not yet a hit-count claim.

## Offline gates and one new oracle counterexample

- The complete repository suite passes 1,567 tests with 5 conditional skips.
  Eight quick source-stateful stages cover 3,840 frames, 480/480 future joins,
  28,543 birth allocations, 10,848 callback-12 changes, and 20,731 callback-14
  changes with zero normalized hits, zero lifecycle drift, and zero failures.
- A separate 3,600-frame gate seed saturates the native bullet pool and
  completes all 450 planner joins, but its independent C differential finds a
  non-lifecycle position error of `1.52587890625e-5` against a forward budget
  of `7.867813110351562e-6`.  RNG and lifecycle state remain exact.  This is
  tracked as AUD-091 and will be minimized/audited rather than hidden by a
  looser tolerance.  Temporary fuzz reports remain outside the repository.

## Paired Easy Final-B rerun cuts the physical hit count in half

- `easy_route2_stage6b_unattended_20260826_051733` is an accepted complete
  Practice run with 12,394 decisions over frames 3..72320, nine native hit
  edges, and zero Bomb input.  It used an isolated prefix and CPUs 8--15 and
  exited normally under deliberately large duration budgets.
- The paired baseline at the same Easy Route-2 Final-B scope recorded 18 hits.
  Per-spell counts improve from 5/8/1/2/1 for 147/151/155/163/167 to
  4/2/0/0/1, while spell 175 gains one hit and nonspell remains one.  The net
  result is a 50-percent reduction, not NMNB.
- The mechanisms moved in the expected direction.  The candidate issued zero
  non-authoritative global queries instead of 10,783; boundary occupancy fell
  from 687/651 bottom/side decisions to 136/296; bottom-eight-pixel pre-hit
  occupancy fell 46.7 percent; nonpositive pre-hit pipeline occupancy fell
  45.3 percent; and the corridor-deadline cluster disappeared.  Input pickup
  remained essentially unchanged, while read and plan p95 improved.
- Seven residual hits are already modeled as robust action-set exhaustion or
  observed overlap.  Two retain positive modeled clearance and no warning and
  are classified as sensing/unmodeled-hazard residuals.  The next work first
  audits those two exact roots, then seeks a cheap generic local
  viable-continuation signal that acts during the existing warning lead.  It
  does not re-enable expensive global work for Easy and does not add a
  stage/spell branch.

## Bullet pool age is now aligned to the final player root

- Source and retained state-5 evidence identify the frame-11417 and
  frame-53738 contacts as existing bullet slots 1156 and 340, not unseen
  births.  Player movement runs at priority 9, bullet motion/collision at
  priority 14, and the collision uses the updated player AABB.
- The controller had combined bullet, enemy, boss, and ECL clocks, then used
  the initial source-to-hazard offset as a negative local bullet projection.
  At frame 53738 this hid a one-frame-old bullet behind a newer boss/ECL read.
- A bullet-specific frame bracket now yields the exact discrete age support.
  A straddled 11413..11414 read against player root 11414 uses `{0,1}`; a
  point 53735 read against root 53736 uses `{1}`.  All local certificate,
  beam, delayed, and issue paths consume the same support.
- Exact physical-root tests change the causal contacts from zero collisions at
  +4.157/+2.267 pixels to one collision at -1.224/-0.214.  At prior frames
  11412 and 53730 the issued directions become unsafe while alternatives
  remain, so the correction can act before contact.
- The retained route contains 9,802 singleton, 2,591 two-age, and one
  three-age reads.  A representative two-age certificate adds about 1.4 ms
  median; singleton overhead is negligible.  AUD-093 and CE-0267 retain the
  evidence and explicit laser/enemy follow-up boundary.
- The complete Linux suite passes 1,568 tests with five conditional skips.
  The unretained 3,600-frame source-stateful gate at seed `0xce0132` passes
  120/120 future joins, 180/180 geometry checks, and 1,414,390 lifecycle
  samples against the independent C oracle with zero lifecycle-position
  error and identical final RNG state/call count.  It saturates the full
  1,536-bullet pool for 85 frames and intentionally produces 90 normalized
  hits, so it exercises a substantially harsher stage without becoming a
  physical survival claim.
