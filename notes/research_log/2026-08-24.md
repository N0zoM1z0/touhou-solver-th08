# TH08 Research Log — 2026-08-24

## Isolated Wine Practice bootstrap exposed score-state lock

- **Observed:** checkpoint `9faa469` passed the isolated Wine smoke, including
  exact retail EXE identity, PE32 native planner, Win32 Python/NumPy,
  no-life-decrement byte, private display, and exact-prefix cleanup.
- **Observed failure:** Stage-5 Practice attempt `20260824_114355` used explicit
  `86400/86700`-second agent/trial budgets and hard no-Bomb, but stopped before
  gameplay at the native stage menu. Route 2/Lunatic/Sakuya-Remilia was exact;
  its Practice availability was only `0x0001`. The compact failed session is
  retained; the ignored Wine report SHA-256 is
  `71eace6836c113b94b57945c629b903c16f1b82341bc9682d18ee429f2a82f39`.
- **Source conclusion:** the external score label was misleading.
  `TitleScreen::OnUpdatePracticeStageSelect` consumes the selected
  `Clrd::difficultiesClearedWithRetries` mask and defaults only Stage 1.
- **Correction:** the Wine Practice wrapper now explicitly opts into a
  data-only, one-bit requested-stage unlock after exact menu/route/difficulty
  validation and requires immediate reread equality. It never patches the EXE
  or changes gameplay resources. Forty-seven focused tests pass; the next gate
  is one fresh Stage-5 physical retry.

## Practice unlock passed; strict scale gate stopped before decisions

- **Observed:** retry `20260824_115523` wrote and reread only the Stage-5 bit
  (`0x0164BA7C: 0x0001→0x0021`), navigated to Stage 5, and armed the agent. It
  then emitted `time_scale_authority_unknown` at frame 1 and produced no
  decision records. This is a failed bootstrap, not a performance sample.
- **Correction:** the isolated runner now exposes the already defined
  Practice-only `--diagnostic-continue-root-only-scale` option. It is explicit
  and remains an unknown-direction constant-current-root proxy; default
  Practice still fails closed. The next retry uses it only to gate local/issue
  latency and hard no-Bomb lifecycle, never source-global correctness.
