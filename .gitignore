audits/
archive/
# Python/tool caches and local environments
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml

# Live trials are large, reproducible sensor captures. Keep compact summaries.
artifacts/runtime_reports/*.jsonl
artifacts/runtime_reports/*.png
artifacts/runtime_reports/*.log
artifacts/runtime_reports/*.root/
artifacts/menu_audit/*.png
artifacts/viability_audit/raw/

# Local, retained research inputs. Their tracked indexes record exact
# provenance; nested repositories and raw native-wind-tunnel runs stay local.
reference/
references/external/
artifacts/native_replay_wind_tunnel/raw/
artifacts/native_snapshot_rolling/raw/
artifacts/wine-th08/

# Raw extraction/decoding output. Existing retained fixtures remain tracked.
artifacts/extracted/
artifacts/decoded/

# Runtime control sentinels and local editor/OS state
*.stop
*.tmp
*.dmp
*.prof
.DS_Store
/image.png

# Rebuildable native planner kernels (Linux .so / Windows .dll)
native/build/
